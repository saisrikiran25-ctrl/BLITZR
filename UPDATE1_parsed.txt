BLITZR: Complete Agent Build Instructions   Master Engineering Document v1.0     SECTION 0: PROJECT OVERVIEW   You are building  BLITZR   —   a campus social - financial exchange. It is a mobile app (React Native)  with a Node.js/TypeScript backend. The app has two markets: a Personal IPO Market (students trade  each other's social clout) and a Prop Market (students bet on campus events). It also   has an  anonymous Rumor Feed that influences market prices.   Tech Stack (non - negotiable):   •   Frontend: React Native (TypeScript)   •   Backend: Node.js with NestJS (TypeScript)   •   Database: PostgreSQL   •   Cache/Queue: Redis   •   Cloud: AWS   •   Real - time:  Socket.io   •   Auth: JWT with college email verification   Existing codebase repo:   saisrikiran25 - ctrl/BLITZR   Read the following files in the repo before starting any task:   •   Blitzr Product Blueprint.txt   •   Blitzr Technical PRD.txt   •   Blitzr Design & Appearance Document.txt   These are your product source of truth. Do not deviate from the design spec or economic model  described in them.    

--- PAGE ---

SECTION 1: DATABASE  —   RUN ALL  MIGRATIONS FIRST   Run these SQL migrations in this exact order before writing any application code.   Migration 1: Institutions Table   CREATE TABLE institutions (      institution_id   UUID PRIMARY KEY DEFAULT gen_random_uuid(),      name             VARCHAR(255) NOT NULL,      short_code       VARCHAR(20) NOT NULL UNIQUE,      email_domain     VARCHAR(100) NOT NULL UNIQUE,      city             VARCHAR(100),      verified         BOOLEAN DEFAULT FALSE,      created_at       TIMESTAMP DEFAULT NOW()   );     --   Seed the first institution   INSERT INTO institutions (name, short_code, email_domain, city, verified)   VALUES ('IIFT Delhi', 'IIFT - D', 'iift.edu', 'Delhi', TRUE);     Migration 2: Modify Users Table   ALTER TABLE users   ADD COLUMN institution_id      UUID REFERENCES institutions(institution_id),   ADD COLUMN   credibility_score   INTEGER NOT NULL DEFAULT 0,   ADD COLUMN tos_accepted        BOOLEAN NOT NULL DEFAULT FALSE,   ADD COLUMN tos_accepted_at     TIMESTAMP;     Migration 3: Modify Tickers Table   ALTER TABLE tickers   ADD COLUMN status   ENUM('ACTIVE', 'AUTO_FROZEN', 'DELISTED', 'MANUAL_FROZEN')                         DEFAULT 'ACTIVE';     Migration 4: Modify Prop Events Table   ALTER TABLE prop_events   ADD COLUMN scope            ENUM('LOCAL', 'REGIONAL', 'NATIONAL') NOT NULL DEFAULT 'LOCAL',   ADD COLUMN institution_id   UUID REFERENCES institutions(institution_id),  

--- PAGE ---

ADD COLUMN featured         BOOLEAN DEFAULT FALSE;   --   institution_id NULL = belongs to ALL campuses (national event)     Migration 5: Modify Rumor Posts Table   ALTER TABLE rumor_posts   ADD COLUMN post_type               ENUM('FACTUAL_CLAIM','OPINION','NEUTRAL') DEFAULT 'NEUTRAL',   ADD COLUMN risk_score              DECIMAL(3,2) DEFAULT 0.0,   ADD COLUMN visibility              ENUM('PUBLIC','PENDING','HIDDEN','REMOVED') DEFAULT 'PUBLIC',   ADD COLUMN moderation_flag         VARCHAR(100) DEFAULT NULL,   ADD COLUMN market_impact_triggered BOOLEAN DEFAULT FALSE,   ADD COLUMN price_snapshot          JSONB DEFAULT '{}';   --   price_snapshot stores ticker prices at time of posting: {"$VIVEK": 120.00}     Migration 6: Post Disputes Table   CREATE TABLE post_disputes (      dispute_id   UUID PRIMARY KEY DEFAULT gen_random_uuid(),      post_id      UUID REFERENCES rumor_posts(post_id) ON DELETE CASCADE,      user_id      UUID REFERENCES users(user_id) ON DELETE CASCADE,      created_at   TIMESTAMP DEFAULT NOW(),      UNIQUE(post_id, user_id)   );     Migration 7: National Leaderboard Table   CREATE TABLE national_leaderboard (      entry_id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),      ticker_id          VARCHAR(50) REFERENCES tickers(ticker_id),      institution_id     UUID REFERENCES institutions(institution_id),      campus_rank        INTEGER,      national_rank      INTEGER,      snapshot_price     DECIMAL(18,4),      snapshot_volume    DECIMAL(18,4),      featured           BOOLEAN DEFAULT FALSE,      computed_at        TIMESTAMP DEFAULT NOW()   );     Migration 8: Moderation Queue Table  

--- PAGE ---

CREATE TABLE moderation_queue (      queue_id       UUID PRIMARY KEY DEFAULT gen_random_uuid(),      flag_type      VARCHAR(100) NOT NULL,      post_id        UUID REFERENCES rumor_posts(post_id),      ticker_id      VARCHAR(50),      meta           JSONB DEFAULT '{}',      status         ENUM('PENDING','REVIEWED_CLEARED','REVIEWED_REMOVED') DEFAULT 'PENDING',      created_at     TIMESTAMP DEFAULT NOW(),      reviewed_at    TIMESTAMP   );     Migration 9: Waitlist Table   CREATE TABLE waitlist (      waitlist_id    UUID PRIMARY KEY DEFAULT gen_random_uuid(),      email          VARCHAR(255) NOT NULL UNIQUE,      email_domain   VARCHAR(100) NOT NULL,      created_at     TIMESTAMP DEFAULT NOW()   );     Migration 10: Admin Analytics Table   CREATE TABLE admin_analytics (      snapshot_id       UUID PRIMARY KEY DEFAULT gen_random_uuid(),      institution_id    UUID REFERENCES institutions(institution_id),      dept_sentiment    JSONB DEFAULT '{}',      avg_score_change  DECIMAL(8,4),      total_trades      INTEGER,      active_users      INTEGER,      flagged_posts     INTEGER,      computed_at       TIMESTAMP DEFAULT NOW()   );       SECTION 2: BACKEND TASKS   Task B1: College Email Verification on Registration   File to modify:   apps/api/src/auth/auth.service.ts  

--- PAGE ---

async register(email: string, password: string) {      // 1. Extract domain from email      const domain = email.split('@')[1];        // 2. Check if domain exists in institutions table      const institution = await this.db.institutions.findOne({        where: { email_domain: domain, verified: true }      });        // 3. If no institution found, add to waitlist and reject      if (!institution) {        await this.db.waitlist.create({          data: { email, email_domain: domain }        });        throw new BadRequestException(          'Your college is not yet on BLITZR. You have been added to the waitlist.'        );      }        // 4. Create user with institution_id linked      const user = await this.db.users.create({        data: {          email,          password: await bcrypt.hash(password, 10),          institution_id: institution.institution_id,          credibility_score: 50, // base score for verified college email          tos_accepted: false        }      });        return user;   }     Task B2: Terms of Service Acceptance Endpoint   Create new endpoint:   POST /api/v1/auth/accept - tos   async acceptTos(userId: string) {      // This endpoint must be called before any other action in the app      await this.db.users.update({        where: { user_id: userId },        data: {          tos_accepted: true,  

--- PAGE ---

       tos_accepted_at: new Date()        }      });   }     // Add a global guard: if user.tos_accepted = false,   // ALL endpoints except /auth/* return 403 with message:   // "Please accept the Terms of Service to continue."     Task B3: Prop Markets  —   Scope Filter   File to modify:   apps/api/src/markets/markets.service.ts   async getMarkets(userId: string, scope: 'LOCAL' | 'REGIONAL' | 'NATIONAL' | 'ALL') {      const user = await this.db.users.findOne({ where: { user_id: userId } });        if (scope === 'LOCAL') {        return this.db.prop_events.findMany({          where: {            institution_id: user.institution_id,            status: 'OPEN'          }        });      }        if (scope === 'NATIONAL') {        return this.db.prop_events.findMany({          where: {            institution_id: null, // null = national            status: 'OPEN'          }        });      }        if (scope === 'ALL') {        return this.db.prop_events.findMany({          where: {            OR: [              { institution_id: user.institution_id },              { institution_id: null }            ],            status: 'OPEN'          }  

--- PAGE ---

     });      }   }     Task B4: Admin  —   Create National Prop Market   Create new endpoint:   POST /api/v1/admin/markets/create   // Protect this with AdminJwtGuard  —   only requests with ADMIN_SECRET in env can call this     async createMarket(dto: CreateMarketDto, isAdmin: boolean) {      // CRITICAL: Enforce scope server - side  —   never trust frontend      const scope = isAdmin ? dto.scope : 'LOCAL';      const institution_id = (scope === 'NATIONAL') ? null : dto.institution_id;        return this.db.prop_events.create({        data: {          title: dto.title,          scope,          institution_id,          options: dto.options,          expiry_timestamp: dto.expiry,          status: 'OPEN',          featured: dto.featured ?? false        }      });   }     Task B5: National Leaderboard Cron Job   Create new file:   apps/api/src/jobs/leaderboard.job.ts   // Run this cron every 30 minutes: @Cron('0 */30 * * * *')     async computeNationalLeaderboard() {      // 1. Delete old snapshot      await this.db.national_leaderboard.deleteMany({});        // 2. Get all verified institutions      const institutions = await this.db.institutions.findMany({        where: { verified: true }      });  

--- PAGE ---

     let nationalRankCounter = 1;        for (const institution of institutions) {        // 3. Get top 3 tickers by total_volume for this institution        const topTickers = await this.db.tickers.findMany({          where: {            owner: { institution_id: institution.institution_id },            status: 'ACTIVE'          },          orderBy: { total_volume: 'desc' },          take: 3,          include: { owner: true }        });          for (let i = 0; i < topTickers.length; i++) {          const ticker = topTickers[i];          const price = this.bondingCurve.getPrice(ticker.current_supply);            await this.db.national_leaderboard.create({            data: {              ticker_id: ticker.ticker_id,              institution_id: institution.institution_id,              campus_rank: i + 1,              national_rank: nationalRankCounter++,              snapshot_price: price,              snapshot_volume: ticker.total_volume,              featured: ticker.featured ?? false            }          });        }      }   }     Create endpoint:   GET /api/v1/leaderboard/national?limit=50   async getNationalLeaderboard(limit: number = 50) {      // Read ONLY from snapshot table  —   never live data      return this.db.national_leaderboard.findMany({        orderBy: [          { featured: 'desc' },   // featured tickers appear first          { national_rank: 'asc' }        ],        take: limit,  

--- PAGE ---

     include: {          ticker: { include: { owner: { select: { display_name: true } } } },          institution: { select: { short_code: true, name: true } }        }      });   }     Task B6: Auto - Freeze Crash Protection Cron Job   Create new file:   apps/api/src/jobs/crash - protection.job.ts   // Run every 5 minutes: @Cron('0 */5 * * * *')     async checkForCrashes() {      const allActiveTickers = await this.db.tickers.findMany({        where: { status: 'ACTIVE' }      });        for (const ticker of allActiveTickers) {        // Get price from 24 hours ago        const txYesterday = await this.db.transactions.findFirst({          where: {            asset_id: ticker.ticker_id,            created_at: { lte: new Date(Date.now()  -   24 * 60 * 60 * 1000) }          },          orderBy: { created_at: 'desc' }        });          if (!txYesterday) continue;          const priceNow = this.bondingCurve.getPrice(ticker.current_supply);        const priceThen = txYesterday.price_at_execution;        const changePct = ((priceNow  -   priceThen) / priceThen) * 100;          if (changePct <=  - 25) {          // Auto - freeze          await this.db.tickers.update({            where: { ticker_id: ticker.ticker_id },            data: { status: 'AUTO_FROZEN' }          });            // Notify owner          await this.notificationService.send(ticker.owner_id, {  

--- PAGE ---

         title: 'Your Clout Score is Protected',            body: 'Your score was moving fast. Markets resume in 60 minutes.'          });            // Notify all holders          const holders = await this.db.transactions.findMany({            where: { asset_id: ticker.ticker_id, tx_type: 'BUY' },            distinct: ['user_id']          });            for (const holder of holders) {            await this.notificationService.send(holder.user_id, {              title: 'Market Paused',              body: `${ticker.ticker_id} is temporarily paused due to high volatility.`            });          }            // Schedule unfreeze after 60 minutes using Redis          await this.redisClient.set(            `unfreeze:${ticker.ticker_id}`,            'true',            'EX', 3600  // 3600 seconds = 60 minutes          );        }      }   }     // Separate job checks for expired freeze keys:   // @Cron('0 * * * * *')  —   runs every minute   async processUnfreezes() {      // Redis keyspace notifications will call this when a key expires      // OR poll manually: get all AUTO_FROZEN tickers and check if 60 min has passed      const frozenTickers = await this.db.tickers.findMany({        where: { status: 'AUTO_FROZEN' }      });        for (const ticker of frozenTickers) {        const key = await this.redisClient.get(`unfreeze:${ticker.ticker_id}`);        if (!key) {          // Key expired = 60 minutes passed = unfreeze          await this.db.tickers.update({            where: { ticker_id: ticker.ticker_id },            data: { status: 'ACTIVE' }          });  

--- PAGE ---

     }      }   }     Task B7: Post Classification Engine   Create new file:   apps/api/src/rumor/classifier.service.ts   // LAYER A: Rule - based classifier (runs first, no API call)   classifyRuleBased(text: string): { post_type: string; confidence: number } {      const lower = text.toLowerCase();        const factualIndicators = [        'just saw', 'i heard', 'confirmed', 'sources say', 'apparently',        'walking into', 'coming out of', 'was at', 'got caught',        'failed', 'expelled', 'arrested', 'cheated', 'broke up',        'escorted', 'suspended', 'rumor is', 'word is'      ];        const opinionIndicators = [        'i think', 'probably', 'might', 'could be', 'in my opinion',        'overrated', 'underrated', 'deserves', 'should be', 'feels like',        'seems like', 'i believe', 'not sure but'      ];        // Check for ticker mentions      const tickerPattern = / \ $[A - Z]{2,15}/g;      const tickers = text.match(tickerPattern) || [];        // Score factual indicators      const factualScore = factualIndicators.filter(i => lower.includes(i)).length;      const opinionScore = opinionIndicators.filter(i => lower.includes(i)).length;        // Ticker + past tense verb = likely factual claim      const pastTensePattern = / \ $(saw|went|was|got|had|did|said|told|heard)/i;      const hasPastTenseWithTicker = tickers.length > 0 && pastTensePattern.test(text);        if (factualScore > 0 || hasPastTenseWithTicker) {        const risk = Math.min(0.4 + (factualScore * 0.15) + (tickers.length * 0.1), 1.0);        return { post_type: 'FACTUAL_CLAIM', confidence: 0.85, risk_score: risk, tickers };      }        if (opinionScore > 0) {  

--- PAGE ---

     return { post_type: 'OPINION', confidence: 0.90, risk_score: 0.1, tickers };      }        return { post_type: 'NEUTRAL', confidence: 0.70, risk_score: 0.05, tickers };   }     // LAYER B: LLM classifier (only called if confidence < 0.8)   async classifyWithLLM(text: string): Promise<{ post_type: string; risk_score: number }> {      // Check cache first      const cacheKey = `classify:${Buffer.from(text).toString('base64').slice(0, 32)}`;      const cached = await this.redisClient.get(cacheKey);      if (cached) return JSON.parse(cached);        const response = await this.openai.chat.completions.create({        model: 'gpt - 4o - mini',        messages: [          {            role: 'system',            content: `You are a content classifier for a campus social app.   Classify the post as one of:   -   FACTUAL_CLAIM: Makes a specific claim about a real person's actions or situation   -   OPINION: Expresses a subjective view or prediction     -   NEUTRAL: General commentary with no specific claim about a person     Return a risk_score from 0.0 to 1.0 where 1.0 = could seriously damage someone's reputation if false.   Return ONLY valid JSON: {"post_type": "...", "risk_score": 0.0}`          },          { role: 'user', content: text }        ],        temperature: 0,        max_tokens: 60      });        const result = JSON.parse(response.choices[0].message.content);        // Cache for 1 hour      await this.redisClient.set(cacheKey, JSON.stringify(result), 'EX', 3600);        return result;   }     // MAIN CLASSIFY FUNCTION  —   call this from the post creation endpoint   async classify(text: string) {      const ruleResult = this.classifyRuleBased(text);  

--- PAGE ---

     if (ruleResult.confidence >= 0.8) {        return ruleResult;      }        // Confidence too low  —   use LLM      return await this.classifyWithLLM(text);   }     Task B8: Post Creation with Full Safety Routing   File to modify:   apps/api/src/rumor/rumor.service.ts   async createPost(userId: string, text: string) {      const user = await this.db.users.findOne({ where: { user_id: userId } });        // STEP 1: Check prohibited content first (Perspective API or keyword list)      const isProhibited = await this.moderationService.checkProhibited(text);      if (isProhibited) {        throw new BadRequestException('This post violates community guidelines.');      }        // STEP 2: Classify the post      const classification = await this.classifierService.classify(text);        // STEP 3: Check credibility gate      if (        classification.post_type === 'FACTUAL_CLAIM' &&        user.credibility_score < 50      ) {        throw new ForbiddenException(          'Make accurate predictions to unlock all post types.'        );      }        // STEP 4: Capture price snapshot for mentioned tickers      const priceSnapshot = {};      for (const ticker of classification.tickers) {        const tickerData = await this.db.tickers.findOne({          where: { ticker_id: ticker }        });        if (tickerData) {          priceSnapshot[ticker] = this.bondingCurve.getPrice(tickerData.current_supply);  

--- PAGE ---

     }      }        // STEP 5: Determine initial visibility      let visibility = 'PUBLIC';      let moderationFlag = null;        if (        classification.post_type === 'FACTUAL_CLAIM' &&        classification.risk_score >= 0.7      ) {        visibility = 'PENDING';        moderationFlag = 'HIGH_RISK_CLAIM';      }        // STEP 6: Save post      const post = await this.db.rumor_posts.create({        data: {          author_id: userId,          text,          post_type: classification.post_type,          risk_score: classification.risk_score,          visibility,          moderation_flag: moderationFlag,          price_snapshot: priceSnapshot,          ghost_id: this.generateGhostId() // existing anonymity logic        }      });        // STEP 7: Add to moderation queue if flagged      if (moderationFlag) {        await this.db.moderation_queue.create({          data: {            flag_type: moderationFlag,            post_id: post.post_id,            meta: { risk_score: classification.risk_score }          }        });        // Notify admin        await this.notificationService.sendAdminAlert(          ` ⚠️   High - risk post pending review. Risk: ${classification.risk_score}`        );      }    

--- PAGE ---

   // STEP 8: Start Market Impact Monitor for posts with tickers      if (classification.tickers.length > 0 && visibility === 'PUBLIC') {        await this.startMarketMonitor(post.post_id, classification.tickers, priceSnapshot);      }        return { post, visibility };   }     Task B9: Market Impact Monitor (Redis - Based)   Create new file:   apps/api/src/rumor/market - monitor.service.ts   async startMarketMonitor(postId: string, tickers: string[], priceSnapshot: object) {      const monitorData = {        ticker_ids: tickers,        price_at_publish: priceSnapshot,        published_at: Date.now(),        checks_done: 0      };        // Store in Redis with 30 - minute TTL      await this.redisClient.set(        `monitor:post:${postId}`,        JSON.stringify(monitorData),        'EX', 1800      );        // Add to the monitoring queue sorted set      // Score = timestamp of next check (2 minutes from now)      await this.redisClient.zadd(        'monitor:queue',        Date.now() + 120000,  // 2 minutes in ms        postId      );   }     // This worker runs every 2 minutes: @Cron('0 */2 * * * *')   async processMonitorQueue() {      const now = Date.now();        // Get all posts due for checking      const postIds = await this.redisClient.zrangebyscore(        'monitor:queue',  

--- PAGE ---

     0,        now      );        for (const postId of postIds) {        // Remove from queue immediately        await this.redisClient.zrem('monitor:queue', postId);          const raw = await this.redisClient.get(`monitor:post:${postId}`);        if (!raw) continue; // TTL expired = 30 min passed = stop monitoring          const monitorData = JSON.parse(raw);        monitorData.checks_done += 1;          let triggered = false;          for (const tickerId of monitorData.ticker_ids) {          const ticker = await this.db.tickers.findOne({            where: { ticker_id: tickerId }          });          if (!ticker) continue;            const priceNow = this.bondingCurve.getPrice(ticker.current_supply);          const priceAtPublish = monitorData.price_at_publish[tickerId];          const changePct = Math.abs((priceNow  -   priceAtPublish) / priceAtPublish * 100);            if (changePct > 15) {            // TRIGGER: Hide post, alert admin            await this.db.rumor_posts.update({              where: { post_id: postId },              data: {                visibility: 'PENDING',                market_impact_triggered: true              }            });              await this.db.moderation_queue.create({              data: {                flag_type: 'MARKET_MANIPULATION_SUSPECTED',                post_id: postId,                ticker_id: tickerId,                meta: {                  price_at_publish: priceAtPublish,                  price_now: priceNow,  

--- PAGE ---

               change_pct: changePct,                  time_elapsed_minutes: Math.floor(                    (Date.now()  -   monitorData.published_at) / 60000                  )                }              }            });              // Notify admin            await this.notificationService.sendAdminAlert(              ` ⚠️   Post may have moved ${tickerId} by ${changePct.toFixed(1)}%. Review needed.`            );              // Notify ticker owner            const ticker_owner = await this.db.tickers.findOne({              where: { ticker_id: tickerId },              include: { owner: true }            });            await this.notificationService.send(ticker_owner.owner_id, {              title: 'Your Clout Score is Protected',              body: 'A post affecting your score is under review.'            });              // Decrease author credibility            await this.db.users.update({              where: { user_id: monitorData.author_id },              data: { credibility_score: { decrement: 20 } }            });              triggered = true;            // Clean up Redis            await this.redisClient.del(`monitor:post:${postId}`);            break;          }        }          if (!triggered && monitorData.checks_done < 15) {          // Update Redis data and re - queue for next check          await this.redisClient.set(            `monitor:post:${postId}`,            JSON.stringify(monitorData),            'EX', 1800          );          await this.redisClient.zadd(  

--- PAGE ---

         'monitor:queue',            Date.now() + 120000,            postId          );        }      }   }     Task B10: Community Dispute System   Create new endpoint:   POST /api/v1/rumor/:postId/dispute   async disputePost(postId: string, userId: string) {      // Prevent duplicate disputes      const existing = await this.db.post_disputes.findOne({        where: { post_id: postId, user_id: userId }      });      if (existing) throw new BadRequestException('You have already disputed this post.');        await this.db.post_disputes.create({        data: { post_id: postId, user_id: userId }      });        // Count total disputes      const disputeCount = await this.db.post_disputes.count({        where: {          post_id: postId,          created_at: { gte: new Date(Date.now()  -   72 * 60 * 60 * 1000) }        }      });        if (disputeCount >= 30) {        // Hide post and queue for review        await this.db.rumor_posts.update({          where: { post_id: postId },          data: { visibility: 'HIDDEN', moderation_flag: 'COMMUNITY_DISPUTED' }        });          await this.db.moderation_queue.create({          data: {            flag_type: 'COMMUNITY_DISPUTED',            post_id: postId,            meta: { dispute_count: disputeCount }  

--- PAGE ---

       }        });          // Penalize author        const post = await this.db.rumor_posts.findOne({ where: { post_id: postId } });        await this.db.users.update({          where: { user_id: post.author_id },          data: { credibility_score: { decrement: 10 } }        });          // Notify author        await this.notificationService.send(post.author_id, {          title: 'Post Under Review',          body: 'Community members have disputed your post. It is under review.'        });      }   }     Task B11: Credibility Score Update Function   Create new file:   apps/api/src/users/credibility.service.ts   // Call these functions at the appropriate points in the codebase     // After a Prop Market bet is settled correctly:   async onPropBetWon(userId: string) {      await this.db.users.update({        where: { user_id: userId },        data: { credibility_score: { increment: 10 } }      });   }     // After a FACTUAL_CLAIM post survives 72 hours without 30 disputes:   async onPostSurvived(postId: string) {      const post = await this.db.rumor_posts.findOne({ where: { post_id: postId } });      await this.db.users.update({        where: { user_id: post.author_id },        data: { credibility_score: { increment: 5 } }      });   }     // After moderator clears a disputed post (vindication):   async onPostCleared(postId: string) {  

--- PAGE ---

   const post = await this.db.rumor_posts.findOne({ where: { post_id: postId } });      // Reward author      await this.db.users.update({        where: { user_id: post.author_id },        data: { credibility_score: { increment: 5 } }      });      // Minor penalty for all disputers (false flagging)      const disputers = await this.db.post_disputes.findMany({        where: { post_id: postId }      });      for (const disputer of disputers) {        await this.db.users.update({          where: { user_id: disputer.user_id },          data: { credibility_score: { decrement: 1 } }        });      }   }     // After moderator removes a post:   async onPostRemoved(postId: string) {      const post = await this.db.rumor_posts.findOne({ where: { post_id: postId } });      await this.db.users.update({        where: { user_id: post.author_id },        data: { credibility_score: { decrement: 15 } }      });   }     Task B12: Trade Execution with Redis Queue (Race Condition Fix)   Create new file:   apps/api/src/trading/trade - queue.service.ts   // Every buy/sell goes through this queue  —   NEVER directly to the database     async enqueueTrade(userId: string, tickerId: string, action: 'BUY' | 'SELL', quantity: number) {      const tradePayload = JSON.stringify({        user_id: userId,        ticker_id: tickerId,        action,        quantity,        timestamp: Date.now()      });        // Each ticker has its own queue  —   this is the key architectural decision  

--- PAGE ---

   await this.redisClient.lpush(`queue:${tickerId}`, tradePayload);        // Return immediately to user  —   UI updates optimistically      return { status: 'QUEUED', message: 'Trade is being processed.' };   }     // One dedicated worker per ticker processes trades sequentially   // Start one worker process per ticker_id using a Bull queue or worker_threads     async processTrade(tickerId: string) {      while (true) {        // BLPOP blocks until an item appears  —   0 timeout = wait forever        const [, raw] = await this.redisClient.blpop(`queue:${tickerId}`, 0);        const trade = JSON.parse(raw);          await this.executeTradeAtomic(trade);      }   }     async executeTradeAtomic(trade: any) {      // Use PostgreSQL transaction  —   safe because we are already sequential per ticker      await this.db.$transaction(async (tx) => {        const ticker = await tx.tickers.findOne({          where: { ticker_id: trade.ticker_id }        });          if (ticker.status !== 'ACTIVE') {          // Notify user: market is frozen          await this.notificationService.send(trade.user_id, {            title: 'Trade Failed',            body: `${trade.ticker_id} market is currently paused.`          });          return;        }          const currentPrice = this.bondingCurve.getPrice(ticker.current_supply);        const user = await tx.users.findOne({ where: { user_id: trade.user_id } });          if (trade.action === 'BUY') {          const cost = this.bondingCurve.getPurchaseCost(ticker.current_supply, trade.quantity);          const tax = cost * 0.03; // 3% burn          const dividend = cost * 0.05; // 5% to owner          const totalCost = cost + tax;    

--- PAGE ---

       if (user.cred_balance < totalCost) {            await this.notificationService.send(trade.user_id, {              title: 'Insufficient Creds',              body: 'You do not have enough Creds for this trade.'            });            return;          }            // Deduct from buyer          await tx.users.update({            where: { user_id: trade.user_id },            data: { cred_balance: { decrement: totalCost } }          });            // Pay dividend to ticker owner          await tx.users.update({            where: { user_id: ticker.owner_id },            data: { cred_balance: { increment: dividend } }          });            // Update supply (3% is burned  —   not added anywhere)          await tx.tickers.update({            where: { ticker_id: trade.ticker_id },            data: {              current_supply: { increment: trade.quantity },              total_volume: { increment: cost }            }          });          } else if (trade.action === 'SELL') {          const saleValue = this.bondingCurve.getSellValue(ticker.current_supply, trade.quantity);          const tax = saleValue * 0.03;          const netPayout = saleValue  -   tax;            await tx.users.update({            where: { user_id: trade.user_id },            data: { cred_balance: { increment: netPayout } }          });            await tx.tickers.update({            where: { ticker_id: trade.ticker_id },            data: {              current_supply: { decrement: trade.quantity },              total_volume: { increment: saleValue }  

--- PAGE ---

         }          });        }          // Log transaction        await tx.transactions.create({          data: {            user_id: trade.user_id,            asset_id: trade.ticker_id,            amount: trade.quantity,            price_at_execution: currentPrice,            tx_type: trade.action          }        });          // Broadcast new price via WebSocket        const newPrice = this.bondingCurve.getPrice(          ticker.current_supply + (trade.action === 'BUY' ? trade.quantity :  - trade.quantity)        );        this.socketService.emit(`price:${trade.ticker_id}`, { price: newPrice });      });   }     Task B13: Admin Analytics Cron Job   Create new file:   apps/api/src/jobs/analytics.job.ts   // Run every 15 minutes: @Cron('0 */15 * * * *')     async computeAdminAnalytics() {      const institutions = await this.db.institutions.findMany({ where: { verified: true } });        for (const institution of institutions) {        const fifteenMinAgo = new Date(Date.now()  -   15 * 60 * 1000);          // Count active users (logged in within last 15 min)        const activeUsers = await this.db.users.count({          where: {            institution_id: institution.institution_id,            last_active_at: { gte: fifteenMinAgo }          }        });    

--- PAGE ---

     // Count flagged posts        const flaggedPosts = await this.db.moderation_queue.count({          where: {            status: 'PENDING',            created_at: { gte: fifteenMinAgo }          }        });          // Avg score change (aggregate  —   NO individual names)        const avgChange = await this.db.transactions.aggregate({          where: {            created_at: { gte: fifteenMinAgo },            user: { institution_id: institution.institution_id }          },          _avg: { price_at_execution: true }        });          await this.db.admin_analytics.create({          data: {            institution_id: institution.institution_id,            active_users: activeUsers,            flagged_posts: flaggedPosts,            avg_score_change: avgChange._avg.price_at_execution ?? 0,            computed_at: new Date()          }        });      }   }       SECTION 3: FRONTEND TASKS   Task F1: Language Reframe  —   Global String Replacement   Instruction to coding agent:   "Do a global find - and - replace across the entire frontend codebase for the following strings. This  is a pure text change  —   no logic changes."   "Buy stock"           → "Boost Score"   "Sell stock"          → "Reduce Position"   "trading people"      → "campus index"  

--- PAGE ---

"your stock price"    → "your Clout Score"   "shareholders"        → "backers"   "IPO yourself"        → "List your Profile"   "Trading Floor"       → "Clout Exchange"  (optional  —   keep if you prefer)   "stock crashed"       → "score dropped"   "pump"                → "boost"   "dump"                → "exit"     Task F2: TOS Screen (Onboarding, Screen 3)   "Add a new screen at position 3 in the onboarding flow. It must appear before the user can  access any part of the app. The screen cannot be skipped."   Screen content:   -   Title: "Before You Enter the Market"   -   Body text (14px, #8E8E93):      "BLITZR is a virtual game economy. Creds and Chips are virtual game        currency with zero real - world monetary value. They cannot be exchanged        for real money under any circumstances."   -   Mandatory unchecked checkbox:      "I understand that Creds and Chips are virtual game currency with no        real - world monetary value."   -   CTA Button: "Enter BLITZR"  —   disabled until checkbox is ticked   -   On tap: call POST /api/v1/auth/accept - tos, then navigate to main app     Task F3: Disclaimer Banner on All Transaction Screens   "Add a persistent, non - dismissible text line at the very bottom of these three screens: Buy  Confirmation, Sell Confirmation, Prop Bet Confirmation."   Text: "BLITZR operates exclusively with virtual credits. No real monetary value. Not a financial product."   Style: font - size: 10px, color: #8E8E93, text - align: center, padding: 8px   Position: below the CTA button, above the system home indicator     Task F4: Arena Tab  —   Scope Selector   "At the top of the Arena (Prop Markets) screen, add a segmented control with three segments:  MY CAMPUS | REGIONAL | NATIONAL. Default selection is MY CAMPUS. When a segment is  selected, re - fetch markets with the corresponding scope parameter. National mark ets must  display a small globe icon (  ) on their card in the top - right corner."  

--- PAGE ---

Task F5: Trading Floor  —   National Leaderboard Row   "Below the existing Top Gainers/Losers section on the Trading Floor, add:"   Section header: "NATIONAL TOP STOCKS" (12px, #8E8E93, letter - spacing: 2px)     A horizontally scrollable FlatList of cards.   Each card (width: 140px, height: 80px, background: #1C1C1E):      -   Row 1: Ticker symbol in white bold (e.g. "$PRIYA")      -   Row 2: College badge in #8E8E93 (e.g. "IIFT - D")       -   Row 3: Price in white monospace      -   Row 4: 24h change in Kinetic Green (#00FF41) or Thermal Red (#FF3B30)     On tap: Open the Dossier screen for that ticker in READ - ONLY mode.   In read - only mode, the BUY and SELL buttons are replaced with a single    grey button labeled "CROSS - CAMPUS TRADING COMING SOON"  —   non - tappable.     Data source: GET /api/v1/leaderboard/national?limit=50   Refresh: every 30 minutes (matches the cron job interval)     Task F6: Rumor Feed  —   Post Type Indicators   "On the Rumor Feed, posts with post_type = 'FACTUAL_CLAIM' must display a small warning  indicator. Style it as:"   A 1px amber/gold (#FFD60A) left border on the post card.   Below the post text, in 10px #8E8E93:   " ⚠   Factual Claim  —   tap flag to dispute"   The flag icon (  ) at the end of this line is tappable.   On tap: call POST /api/v1/rumor/:postId/dispute   After tapping: icon turns grey, label changes to "Disputed"     Task F7: Credibility Score Posting Gate UI   "In the post input bar at the bottom of the Rumor Feed:"   If user.credibility_score < 50:      Show a small lock icon (  ) next to the send button.      When user types a FACTUAL_CLAIM (detect client - side using the same keyword       list from the backend classifier) and taps send:      Show a bottom sheet with:        Title: "Unlock Factual Claims"  

--- PAGE ---

     Body: "Make accurate Arena predictions to unlock the ability to post                factual claims. Your current Credibility Score: {score}/50"        Button: "Go to Arena" → navigates to Arena tab        If user.credibility_score >= 50:      No lock icon. Normal posting flow.       SECTION 4: DEAN'S DASHBOARD (SEPARATE  WEB APP)   "Build a separate Next.js web application. This is NOT part of the React Native app. It is a web  portal at a separate URL. It has its own login system."   Setup   npx create - next - app@latest blitzr - admin  -- typescript  -- tailwind     Page 1: Login   -   Single email + password form   -   Only accounts with role = 'ADMIN' or role = 'INSTITUTION_ADMIN' in the users       table can log in here   -   Uses a separate JWT secret (ADMIN_JWT_SECRET in env)     Page 2: Campus Pulse Dashboard   Data source: GET /api/v1/admin/analytics (reads ONLY from admin_analytics table)     Display:   -   Line chart: avg_score_change over last 7 days (use Recharts library)   -   Stat cards: Active Users (last 15 min), Flagged Posts (pending), Total Trades today   -   Department heatmap: aggregate score by dept (use existing heatmap logic from student app)   -   NO individual student names or tickers shown anywhere on this page   -   Label: "Campus Pulse Report  —   Aggregated & Anonymized"     Page 3: Prediction Intelligence  

--- PAGE ---

Data source: GET /api/v1/markets?scope=LOCAL (admin auth token auto - includes institution)     Display:   -   Table: Event Title | Yes% | No% | Total Chips | Expiry   -   Each row has a visual Yes/No bar (green/red split)   -   Read - only. No betting controls.   -   Label: "Student Prediction Intelligence  —   Powered by BLITZR"     Page 4: Content Safety Report   Data source: GET /api/v1/admin/moderation - queue?status=PENDING     Display:   -   Table: Timestamp | Flag Type | Status | Action   -   Post content is NOT shown by default   -   Each row has two buttons: "Clear" and "Remove"      -   Clear → PATCH /api/v1/admin/moderation/:queueId/clear                 → Sets post.visibility = 'PUBLIC', calls onPostCleared()      -   Remove → PATCH /api/v1/admin/moderation/:queueId/remove                  → Sets post.visibility = 'REMOVED', calls onPostRemoved()     Page 5: Emergency Controls   Two controls:     1. "Pause All Campus Markets" button (large, red background):       → POST /api/v1/admin/campus/pause       → Sets ALL tickers for this institution to status = 'MANUAL_FROZEN'       → Sets ALL prop_events for this institution to status = 'PAUSED'       → Auto - resumes after 24 hours via cron       → Requires typing "CONFIRM PAUSE" in a text field before the button activates     2. Manual Delist by Email:       → Input field: "Student college email"       → On submit: looks up user by email, triggers delist flow for their ticker       → Shows confirmation: "Ticker $RAHUL has been delisted and all          backers refunded."       SECTION 5: BUILD ORDER  

--- PAGE ---

Execute tasks in this exact sequence. Do not proceed to the next group until the current group is  complete and tested.   GROUP 1  —   Foundation (Days 1 - 3):      Migration 1 - 10 (all database changes)      Task B1 (college email verification)      Task B2 (TOS acceptance)      Task F2 (TOS screen)     GROUP 2  —   Safety Systems (Days 4 - 8):      Task B7 (classifier service)      Task B8 (post creation with routing)      Task B10 (dispute system)      Task B11 (credibility score functions)      Task F6 (post type indicators)      Task F7 (credibility gate UI)     GROUP 3  —   Trading Engine (Days 8 - 12):      Task B12 (Redis trade queue)      Task B6 (crash protection cron)      Task F3 (disclaimer banners)      Task F1 (language reframe)     GROUP 4  —   League Architecture (Days 12 - 18):      Task B3 (scope filter for markets)      Task B4 (admin market creation)      Task B5 (national leaderboard cron)      Task F4 (scope selector UI)      Task F5 (national leaderboard row)     GROUP 5  —   Dean's Dashboard (Days 18 - 25):      Task B13 (analytics cron)      All Dean's Dashboard pages     GROUP 6  —   Market Impact Monitor (Days 25 - 28):      Task B9 (Redis market monitor)     DO NOT LAUNCH BEFORE GROUP 1, 2, AND 3 ARE COMPLETE.   Groups 4, 5, and 6 can ship as updates after launch.      

--- PAGE ---

SECTION 6: ENVIRONMENT VARIABLES  REQUIRED   # Database   DATABASE_URL=postgresql://...     # Redis   REDIS_URL=redis://...     # Auth   JWT_SECRET=your_jwt_secret   ADMIN_JWT_SECRET=your_admin_jwt_secret_different_from_above     # AI Classification   OPENAI_API_KEY=your_openai_key     # Notifications   FCM_SERVER_KEY=your_firebase_key     # Bonding Curve   BONDING_CURVE_K=0.005  # Adjust per campus size per Technical PRD     # App   NODE_ENV=production   PORT=3000   ADMIN_PORT=3001       End of master build document. All logic, all endpoints, all database changes, and all UI  tasks are specified above. Build in the order given in Section 5.  